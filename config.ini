[Logging]
# Logging level: DEBUG, INFO, WARNING, ERROR, CRITICAL
level = INFO
format = %%(asctime)s - %%(name)s - %%(levelname)s - %%(message)s

[Processing]
# Maximum number of segments to process in one batch
batch_size = 1000

[Output]
# Encoding for the output .eaf file
encoding = UTF-8
